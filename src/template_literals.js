// template literals are used to concatenate strings together using back ticks 
const name = 'Isaac';
const country = "Japan";
const age = 33;

console.log(`Name ${name.length} Country ${country} Age ${age}`);